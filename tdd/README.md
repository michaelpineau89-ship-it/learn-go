# TDD Workspace

Use this folder for exercises from Learn Go with Tests:
https://quii.gitbook.io/learn-go-with-tests

Suggested structure:
- one folder per chapter, each with its own package and tests
- run tests from repo root with `go test ./...`
